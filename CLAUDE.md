# Parable

Recursive descent bash parser in pure Python.

## Commands

```bash
just test        # Run tests (Python 3.14)
just test-all    # All Python versions (3.10-3.14) — required before committing
```
