[project]
name = "parable"
version = "0.1.0"
description = "Bash parser"
requires-python = ">=3.11"

[project.scripts]
parable-test = "parable.run_tests:main"

[build-system]
requires = ["uv_build>=0.7,<0.8"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-root = "src"
