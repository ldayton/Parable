[project]
name = "parable"
version = "0.1.0"
description = "Bash parser"
requires-python = ">=3.11"

[project.scripts]
parable-test = "parable.run_tests:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/parable"]

[tool.hatch.build.hooks.custom]
