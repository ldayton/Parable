# Brace edge cases from GNU Bash test suite
# See docs/roadmap.md

# === Literal braces in argument position ===
# { and } are reserved words only at command position

=== echo { }
echo { }
---
(command (word "echo") (word "{") (word "}"))

=== echo }
echo }
---
(command (word "echo") (word "}"))

=== echo {
echo {
---
(command (word "echo") (word "{"))

=== brace group with } argument
{ echo }; }
---
(brace-group (list (command (word "echo") (word "}")) (semi)))

# === Braces in parameter expansion defaults ===

=== braces in parameter expansion default
echo ${abc:-G { I } K }
---
(command (word "echo") (word "${abc:-G { I } K }" (param "abc" ":-" "G { I } K ")))

=== braces in parameter expansion default with value set
echo ${abc:-{ foo }}
---
(command (word "echo") (word "${abc:-{ foo }}" (param "abc" ":-" "{ foo }")))
