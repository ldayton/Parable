[project]
name = "oracle"
version = "0.1.0"
description = "Bash oracle utilities for Parable"
requires-python = ">=3.14"

[project.scripts]
oracle = "oracle:main"

[build-system]
requires = ["uv_build>=0.9.26"]
build-backend = "uv_build"
